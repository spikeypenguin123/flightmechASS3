function G_body = Gravity(aircraft,q,m)
    % Inputs:
    % g: gravity (m/s^2)
    % 4x1 quaternions q, mass m (kg)
    % out: 3x1 G_body (N)
    
    G_body = aircraft.inertial.g*aircraft.inertial.m*[0 0 1]*DCM(q)';
    
end
